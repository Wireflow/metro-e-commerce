import PageHeader from '@/components/layout/PageHeader';

type Props = {};

const StoreUsersPage = (props: Props) => {
  return (
    <div>
      <PageHeader title="Users" description="Manage your store users" />
    </div>
  );
};

export default StoreUsersPage;
